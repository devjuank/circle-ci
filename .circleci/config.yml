version: 2.1

parameters:
  name:
    type: string
    default: juank-pipeline-default

jobs:
  sayhello:
    parameters:
      name:
        type: string
        default: juank-job-default
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo hola << parameters.name >>

workflows:
  workflow1:
    jobs:
      - sayhello:
          name: juank
  workflow2:
    jobs:
      - sayhello        